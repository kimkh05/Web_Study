const intNum = 10; // Number
const floatNum = 0.1;

const singleQuoteStr = 'single quote string'; //String
const doubleQuoteSte = "double quote string";
const singleChar = 'a';

const boolVar = true; //Boolean

let emptyVar; //Undefined

const nullVar = null; //NULL

console.log(
    typeof intNum,
    typeof floatNum,
    typeof singleQuoteStr,
    typeof doubleQuoteSte,
    typeof singleChar,
    typeof boolVar,
    typeof emptyVar,
    typeof nullVar
)